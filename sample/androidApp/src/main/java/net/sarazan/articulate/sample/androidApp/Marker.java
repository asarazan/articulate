package net.sarazan.articulate.sample.androidApp;

/**
 * Compiles only if R.string.hello resolves -- proof that the strings
 * authored once in :i18n reached this module's generated resources through
 * the real Android edge, not merely that some task ran.
 */
public final class Marker {
    public static final int HELLO_STRING_ID = R.string.hello;

    private Marker() {}
}
