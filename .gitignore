# Gradle
.gradle/
build/
# JVM heap dumps -- Gradle writes these on OOM and they are enormous
*.hprof

# IDE
.idea/
*.iml
local.properties

# Agent / editor scratch — but agent definitions are project config and versioned
.claude/*
!.claude/agents/

# macOS
.DS_Store
